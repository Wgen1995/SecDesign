# A1.2 认证方式变更

## 匹配关键词
密码、Token、MFA、OAuth、SAML、SSO、认证、Auth、Login

## STRIDE适用
S T I D E

## 安全检查点

### 认证方式变更
- [ ] 认证必须强制TLS传输（phase: deploy）
- [ ] 暴力破解防护（速率限制、锁定策略、指数退避）（phase: design）
- [ ] 多因素认证（MFA）覆盖高敏感操作（phase: design）
- [ ] OAuth/SAML回调URL必须白名单校验（phase: code）
- [ ] Token过期机制（Access Token短过期 + Refresh Token轮换）（phase: code）
- [ ] 登录失败不泄露内部信息（不区分用户名/密码错误）（phase: test）

## 关联知识层
- KL-3: OWASP A07 (Auth Failures); API2 (Broken Authentication); ASVS V2.1 (认证架构)
- KL-5: 无明确映射（参考 data/credential-exposure 关联Token管理）
- KL-6: G1 (凭证管理) — G1-04 (凭证加密传输)
